# 01 November 2019
# CSC121 M5HW – DataFrame
# Dustin Cooksey
# 
# 

import pandas as p
import numpy as n


def main():
    
    gradebook_dict = {}
    user_tests = input(int('How many test are you storing? '))

    user_students = input(int('How many students took the test? '))
    
    for user_students in keys:
        new_key = input('Please enter the students name: ')
        gradebook_dict[new_key]
        for user_tests in values:
            new_test = input('Please enter the test grades: ')
            gradebook_dict[new_key] = new_test


main()
